# 🎨 Інструкція для дизайнера: Робота з Pinegrow 9 + Astro

> **Для кого:** Дизайнери, які знають Figma, але не знають програмування
> **Мета:** Навчитися візуально редагувати дизайн сайту храму в Pinegrow
> **Час навчання:** 30-60 хвилин

---

## 📋 Зміст

1. [Що таке Pinegrow і навіщо він потрібен](#що-таке-pinegrow)
2. [Початок роботи: Відкриття проекту](#відкриття-проекту)
3. [Інтерфейс Pinegrow: Де що знаходиться](#інтерфейс)
4. [Базові поняття: Tailwind CSS](#tailwind-css)
5. [Редагування сторінок](#редагування-сторінок)
6. [Робота з кольорами](#кольори)
7. [Робота з текстом і шрифтами](#типографіка)
8. [Відступи та розміри](#spacing)
9. [Responsive дизайн (мобільна версія)](#responsive)
10. [Що можна робити, а що не можна](#обмеження)
11. [Збереження змін](#збереження)
12. [Типові проблеми та рішення](#troubleshooting)

---

## 🎯 Що таке Pinegrow і навіщо він потрібен {#що-таке-pinegrow}

### Проста аналогія

**Figma** — це як фотошоп для веб-дизайну (малюєте красивий макет)
**Pinegrow** — це як конструктор LEGO для готового сайту (збираєте з реальних блоків)

### Що ви будете робити

- ✅ Змінювати кольори елементів
- ✅ Редагувати текст
- ✅ Міняти відступи (padding, margin)
- ✅ Додавати тіні, рамки, заокруглення
- ✅ Налаштовувати мобільну версію
- ✅ Створювати нові секції

### Що НЕ потрібно робити

- ❌ Писати код вручну
- ❌ Розуміти JavaScript
- ❌ Знати HTML (але базове розуміння допоможе)

---

## 🚀 Початок роботи: Відкриття проекту {#відкриття-проекту}

### Крок 1: Запуск Astro dev сервера

**Це важливо зробити ПЕРЕД відкриттям Pinegrow!**

1. Відкрийте **командний рядок** (Command Prompt) або **Terminal**
2. Перейдіть у папку проекту:
   ```bash
   cd D:\Projects\Three_Holy_Hierarchs
   ```
3. Запустіть сервер розробки:
   ```bash
   npm run dev
   ```
4. Ви побачите:
   ```
   astro v5.17.1 ready in 2000 ms
   Local    http://127.0.0.1:4321/
   ```
5. **НЕ ЗАКРИВАЙТЕ це вікно!** Сервер має працювати весь час, поки ви редагуєте.

### Крок 2: Відкриття проекту в Pinegrow 9

1. Запустіть **Pinegrow Web Editor 9**
2. У меню оберіть:
   ```
   File → Open Project...
   ```
3. Виберіть папку проекту:
   ```
   D:\Projects\Three_Holy_Hierarchs
   ```
4. Pinegrow автоматично розпізнає Astro проект
5. Зліва з'явиться дерево файлів

### Крок 3: Відкриття сторінки для редагування

**Структура проекту:**
```
📁 src/
  ├── 📁 pages/              ← ТУТ СТОРІНКИ САЙТУ
  │   ├── index.astro        ← Головна сторінка
  │   ├── about.astro        ← Про храм
  │   ├── schedule.astro     ← Розклад богослужінь
  │   ├── contacts.astro     ← Контакти
  │   ├── 📁 saints/
  │   │   └── index.astro    ← Святі покровителі
  │   └── 📁 sermons/
  │       └── index.astro    ← Проповіді
  │
  └── 📁 components/         ← ТУТ КОМПОНЕНТИ
      └── 📁 widgets/        ← Багаторазові блоки
```

**Для початку відкрийте головну сторінку:**
1. У дереві файлів знайдіть: `src/pages/index.astro`
2. Двічі клікніть — сторінка відкриється в центральній панелі

---

## 🖥️ Інтерфейс Pinegrow: Де що знаходиться {#інтерфейс}

### Основні панелі

```
┌─────────────────────────────────────────────────────────┐
│  Меню: File, Edit, View, Tools...                      │
├──────────┬─────────────────────────────┬────────────────┤
│          │                             │                │
│  ЛІВА    │      ЦЕНТР                  │    ПРАВА       │
│  ПАНЕЛЬ  │      (Canvas)               │    ПАНЕЛЬ      │
│          │                             │                │
│  Дерево  │   Тут ви бачите сайт       │  Налаштування  │
│  файлів  │   і клікаєте на елементи   │  вибраного     │
│          │                             │  елемента      │
│          │                             │                │
└──────────┴─────────────────────────────┴────────────────┘
```

### Ліва панель (Tree View)

**Показує:**
- 📁 Структуру файлів проекту
- 🔖 HTML структуру відкритої сторінки

**Як користуватися:**
- Клік на файл → відкрити сторінку
- Клік на елемент у дереві → виділити на сторінці

### Центральна панель (Canvas)

**Показує:**
- 👀 Візуальний вигляд сайту
- 🖱️ Можна клікати на елементи для редагування

**Панель інструментів зверху:**
```
[Desktop] [Tablet] [Mobile]  ← Перемикач розмірів екрану
[←] [→] [↻]                  ← Назад/Вперед/Оновити
```

### Права панель (Properties)

**Показує:**
- 🎨 Tailwind CSS налаштування
- 📐 Layout (розташування)
- 🎭 Стилі (кольори, шрифти, тощо)

**Вкладки:**
- **Style** — Tailwind класи
- **Properties** — HTML атрибути
- **Actions** — події та анімації

---

## 🎨 Базові поняття: Tailwind CSS {#tailwind-css}

### Що таке Tailwind?

Це набір **готових класів** для стилізації. Замість писати CSS код, ви додаєте класи.

### Аналогія з Figma

| Figma | Tailwind | Що це робить |
|-------|----------|--------------|
| Fill: Blue | `bg-blue-500` | Синій фон |
| Padding: 16px | `p-4` | Відступ всередині |
| Border Radius: 8px | `rounded-lg` | Заокруглені кути |
| Shadow: Drop Shadow | `shadow-xl` | Тінь |

### Система spacing (відступів)

Tailwind використовує числа від 0 до 96:

| Число | Розмір | Використання |
|-------|--------|--------------|
| `1` | 4px | Дуже маленький відступ |
| `2` | 8px | Маленький |
| `4` | 16px | Середній (стандарт) |
| `6` | 24px | Великий |
| `8` | 32px | Дуже великий |
| `16` | 64px | Величезний |

**Приклад:**
- `p-4` = padding: 16px (відступ всередині)
- `m-8` = margin: 32px (відступ зовні)
- `gap-6` = gap: 24px (проміжок між елементами)

---

## ✏️ Редагування сторінок {#редагування-сторінок}

### Основний workflow

1. **Виберіть елемент** (клік на сторінці або в дереві)
2. **Знайдіть потрібну властивість** у правій панелі
3. **Змініть значення**
4. **Збережіть** (Ctrl+S або Cmd+S)

### Приклад 1: Змінити колір фону

**Завдання:** Зробити фон секції золотим

1. Клікніть на секцію (наприклад, Hero)
2. У правій панелі → **Style** → **Background**
3. Знайдіть поле **Color**
4. Виберіть `bg-primary-500` (золотий)
5. Збережіть (Ctrl+S)

**До:**
```html
<div class="bg-white">
```

**Після:**
```html
<div class="bg-primary-500">
```

### Приклад 2: Змінити розмір тексту

**Завдання:** Збільшити заголовок

1. Клікніть на заголовок (h1)
2. У правій панелі → **Style** → **Typography**
3. Знайдіть **Font Size**
4. Змініть на `text-6xl` (дуже великий)
5. Збережіть

**Розміри тексту:**
- `text-sm` — маленький (14px)
- `text-base` — нормальний (16px)
- `text-lg` — великий (18px)
- `text-xl` — дуже великий (20px)
- `text-2xl` — величезний (24px)
- `text-4xl` — гігантський (36px)
- `text-6xl` — мега (60px)

### Приклад 3: Додати тінь до картки

**Завдання:** Зробити картку об'ємною

1. Клікніть на картку (div з класом card)
2. У правій панелі → **Style** → **Effects**
3. Знайдіть **Shadow**
4. Виберіть `shadow-xl` (велика тінь)
5. Збережіть

**Типи тінь:**
- `shadow-sm` — мала
- `shadow` — стандартна
- `shadow-md` — середня
- `shadow-lg` — велика
- `shadow-xl` — дуже велика
- `shadow-2xl` — величезна

---

## 🎨 Робота з кольорами {#кольори}

### Колірна схема проекту (православна тематика)

**Головні кольори:**

| Назва | Клас Tailwind | Hex | Використання |
|-------|---------------|-----|--------------|
| **Золотий** (Primary) | `primary-500` | #EAB308 | Акценти, кнопки, іконки |
| **Темний золотий** | `primary-600` | #CA8A04 | Hover стани |
| **Темно-синій** | `secondary-600` | #1E3A5F | Заголовки, фон |
| **Дуже темно-синій** | `secondary-900` | #1A237E | Текст, footer |
| **Червоний літургійний** | `accent` | #8B0000 | Святкові акценти |

### Як використовувати кольори

**1. Колір фону:**
```
bg-primary-500      → золотий фон
bg-secondary-900    → темно-синій фон
bg-white            → білий фон
bg-gray-50          → світло-сірий
```

**2. Колір тексту:**
```
text-primary-500    → золотий текст
text-secondary-900  → темно-синій текст
text-white          → білий текст
text-gray-600       → сірий текст
```

**3. Колір рамки:**
```
border-primary-500  → золота рамка
border-accent       → червона рамка
```

### Практичний приклад: Кнопка

**Створимо красиву кнопку:**

1. Виберіть кнопку (button або a)
2. Додайте класи у правій панелі:

```
Background: bg-primary-500
Text Color: text-white
Padding X: px-6
Padding Y: py-3
Border Radius: rounded-lg
Shadow: shadow-lg
Font Weight: font-semibold
```

**Hover ефект (при наведенні миші):**
```
Hover:Background: hover:bg-primary-600
```

**Результат:**
```html
<button class="bg-primary-500 hover:bg-primary-600 text-white px-6 py-3 rounded-lg shadow-lg font-semibold">
  Записатися
</button>
```

---

## 📝 Робота з текстом і шрифтами {#типографіка}

### Шрифти проекту

**1. Playfair Display** — для заголовків (елегантний, класичний)
- Клас: `font-heading`
- Використання: h1, h2, h3

**2. Inter** — для основного тексту (читабельний, сучасний)
- Клас: `font-body`
- Використання: параграфи, кнопки, меню

### Налаштування тексту

**Розмір (Font Size):**
```
text-sm     → 14px (маленький текст)
text-base   → 16px (звичайний параграф)
text-lg     → 18px (великий параграф)
text-xl     → 20px (підзаголовок)
text-2xl    → 24px (h3)
text-3xl    → 30px (h2)
text-4xl    → 36px (h1)
text-6xl    → 60px (hero заголовок)
```

**Товщина (Font Weight):**
```
font-light      → тонкий (300)
font-normal     → звичайний (400)
font-medium     → середній (500)
font-semibold   → напівжирний (600)
font-bold       → жирний (700)
```

**Вирівнювання (Text Align):**
```
text-left       → зліва
text-center     → по центру
text-right      → справа
text-justify    → по ширині
```

**Колір тексту (Text Color):**
```
text-gray-600       → сірий (звичайний текст)
text-secondary-900  → темно-синій (заголовки)
text-primary-500    → золотий (акценти)
text-white          → білий (на темному фоні)
```

**Висота рядка (Line Height):**
```
leading-tight       → щільний текст
leading-normal      → звичайний
leading-relaxed     → просторий (для довгих текстів)
```

### Практичний приклад: Заголовок Hero

**Завдання:** Створити великий заголовок на головній

1. Виберіть h1 у Hero секції
2. Додайте класи:

```
Font Family: font-heading
Font Size: text-6xl
Font Weight: font-bold
Text Color: text-secondary-900
Line Height: leading-tight
```

**Результат:**
```html
<h1 class="font-heading text-6xl font-bold text-secondary-900 leading-tight">
  Храм на честь Трьох святителів
</h1>
```

---

## 📏 Відступи та розміри {#spacing}

### Типи відступів

**1. Padding (p) — відступ ВСЕРЕДИНІ елемента**
```
p-4     → padding: 16px (з усіх боків)
px-4    → padding-left + padding-right: 16px
py-4    → padding-top + padding-bottom: 16px
pt-4    → padding-top: 16px
pb-4    → padding-bottom: 16px
pl-4    → padding-left: 16px
pr-4    → padding-right: 16px
```

**2. Margin (m) — відступ ЗОВНІ елемента**
```
m-4     → margin: 16px (з усіх боків)
mx-auto → margin-left + margin-right: auto (центрування)
my-8    → margin-top + margin-bottom: 32px
mt-4    → margin-top: 16px
mb-4    → margin-bottom: 16px
```

**3. Gap — проміжок МІЖ елементами (у flexbox/grid)**
```
gap-4   → 16px між елементами
gap-6   → 24px між елементами
```

### Візуальна шпаргалка

```
┌─────────────────────────────────────┐
│ m-4 (margin - зовнішній відступ)    │
│ ┌─────────────────────────────────┐ │
│ │ p-4 (padding - внутрішній)      │ │
│ │ ┌─────────────────────────────┐ │ │
│ │ │                             │ │ │
│ │ │   КОНТЕНТ                   │ │ │
│ │ │                             │ │ │
│ │ └─────────────────────────────┘ │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

### Практичний приклад: Картка новини

**Завдання:** Створити картку з правильними відступами

```html
<div class="bg-white rounded-lg shadow-lg p-6 m-4">
  ↑ p-6 = відступ всередині 24px
  ↑ m-4 = відступ зовні 16px

  <h3 class="text-2xl font-bold mb-4">Заголовок новини</h3>
         ↑ mb-4 = відступ знизу 16px

  <p class="text-gray-600 leading-relaxed">Текст новини...</p>
</div>
```

### Швидкі рецепти

**Центрування блоку:**
```
mx-auto max-w-7xl
```

**Великі відступи для секцій:**
```
py-16    → 64px зверху та знизу
px-4     → 16px зліва та справа
```

**Картка з відступами:**
```
p-6      → 24px всередині
space-y-4 → 16px між дочірніми елементами
```

---

## 📱 Responsive дизайн (мобільна версія) {#responsive}

### Breakpoints (точки переходу)

Tailwind використовує префікси для різних розмірів екрану:

| Префікс | Розмір | Пристрій |
|---------|--------|----------|
| (нічого) | 0-640px | Мобільний (за замовчуванням) |
| `sm:` | 640px+ | Великий мобільний |
| `md:` | 768px+ | Планшет |
| `lg:` | 1024px+ | Ноутбук |
| `xl:` | 1280px+ | Desktop |

### Як це працює: Mobile First

**Правило:** Спочатку стилізуйте для мобільного, потім додавайте для більших екранів.

**Приклад:**
```html
<div class="text-2xl md:text-4xl lg:text-6xl">
  Заголовок
</div>
```

**Що це означає:**
- На мобільному (0-768px): `text-2xl` (24px)
- На планшеті (768px+): `text-4xl` (36px)
- На десктопі (1024px+): `text-6xl` (60px)

### Перемикання видів у Pinegrow

**Панель зверху Canvas:**
```
[Desktop] [Tablet] [Mobile]
```

1. Клікніть **Mobile** — бачите мобільну версію
2. Клікніть **Tablet** — бачите планшет
3. Клікніть **Desktop** — бачите повну версію

### Типові responsive паттерни

**1. Приховати на мобільному:**
```
hidden md:block
```
- `hidden` — сховано на мобільному
- `md:block` — показати на планшеті та більше

**2. Колонки → рядки:**
```
flex flex-col md:flex-row
```
- Мобільний: елементи в колонку (вертикально)
- Планшет+: елементи в ряд (горизонтально)

**3. Розмір тексту:**
```
text-3xl md:text-5xl lg:text-7xl
```
- Мобільний: 30px
- Планшет: 48px
- Desktop: 72px

**4. Padding різний:**
```
p-4 md:p-8 lg:p-16
```
- Мобільний: 16px
- Планшет: 32px
- Desktop: 64px

### Практичний приклад: Hero секція

**Завдання:** Адаптувати Hero під всі пристрої

```html
<div class="
  py-16 md:py-24 lg:py-32
  px-4 md:px-8
">
  <h1 class="
    text-4xl md:text-5xl lg:text-7xl
    font-bold
    text-center
  ">
    Храм Трьох святителів
  </h1>

  <p class="
    text-lg md:text-xl
    mt-4 md:mt-6
    max-w-2xl mx-auto
    text-center
  ">
    Опис храму...
  </p>
</div>
```

**Що робить цей код:**
- Відступи збільшуються на більших екранах
- Текст росте від мобільного до desktop
- Параграф центрується та обмежується шириною

---

## ⚠️ Що можна робити, а що не можна {#обмеження}

### ✅ Можна робити

**Візуальне редагування:**
- ✅ Змінювати Tailwind класи
- ✅ Редагувати статичний текст
- ✅ Додавати/видаляти HTML елементи
- ✅ Змінювати структуру сторінки
- ✅ Додавати нові секції
- ✅ Копіювати блоки з інших сторінок
- ✅ Налаштовувати responsive
- ✅ Працювати з зображеннями

**Tailwind:**
- ✅ Кольори фону та тексту
- ✅ Відступи (padding, margin)
- ✅ Тіні, рамки, заокруглення
- ✅ Типографіка (розмір, вага, вирівнювання)
- ✅ Layout (flexbox, grid)
- ✅ Hover ефекти

### ⚠️ Обережно з цим

**Frontmatter блок (НЕ ЧІПАЙТЕ!):**
```astro
---
// ЦЕЙ БЛОК НЕ РЕДАГУЙТЕ!
import Layout from '~/layouts/PageLayout.astro';
const metadata = { title: 'Головна' };
---
```

**Динамічні змінні:**
```astro
{title}           ← НЕ редагуйте
{metadata.title}  ← НЕ редагуйте
{props.subtitle}  ← НЕ редагуйте
```

**Astro компоненти:**
```astro
<Hero />          ← Можна редагувати атрибути
<Features />      ← Можна редагувати props
<Content />       ← Але не видаляйте!
```

### ❌ Не можна/не треба

- ❌ Видаляти frontmatter блок (`---`)
- ❌ Редагувати JavaScript логіку
- ❌ Видаляти `{змінні}` в тексті
- ❌ Змінювати імпорти компонентів
- ❌ Чіпати файли у `src/utils/` або `src/lib/`

### 🛡️ Правило безпеки

**ЗОЛОТЕ ПРАВИЛО:** Якщо ви не розумієте, що робить код — не чіпайте його!

**Безпечно редагувати:**
- HTML теги: `<div>`, `<h1>`, `<p>`, `<button>`, etc.
- Tailwind класи: `class="..."`
- Статичний текст між тегами

**НЕ чіпати:**
- Все між `---` та `---`
- Все між `{` та `}`
- Імпорти на початку файлу

---

## 💾 Збереження змін {#збереження}

### Як зберегти зміни

**Спосіб 1: Швидке збереження**
```
Windows: Ctrl + S
Mac: Cmd + S
```

**Спосіб 2: Через меню**
```
File → Save
```

### Що відбувається при збереженні

1. **Pinegrow зберігає файл** → `.astro` файл оновлюється
2. **Astro dev server бачить зміни** → перезбирає сторінку
3. **Браузер автоматично оновлюється** → ви бачите результат

### Перевірка результату

**У браузері:**
1. Відкрийте `http://localhost:4321`
2. Після збереження у Pinegrow сторінка оновиться автоматично
3. Якщо ні — оновіть вручну (F5 або Ctrl+R)

### Відміна змін

**Якщо щось пішло не так:**

**Спосіб 1: Undo (скасувати)**
```
Ctrl + Z (Windows)
Cmd + Z (Mac)
```

**Спосіб 2: Git (якщо зіпсували файл повністю)**
```
Попросіть розробника відкотити зміни через Git
```

---

## 🎓 Покрокові туторіали

### Туторіал 1: Змінити кольори головної сторінки

**Мета:** Зробити Hero секцію з градієнтним фоном

**Кроки:**

1. **Відкрийте** `src/pages/index.astro`
2. **Знайдіть** Hero секцію (великий блок зверху)
3. **Клікніть** на неї
4. **У правій панелі** → Style → Background
5. **Видаліть** старі класи фону
6. **Додайте:**
   ```
   bg-gradient-to-br
   from-secondary-900
   to-primary-600
   ```
7. **Збережіть** (Ctrl+S)
8. **Перевірте** у браузері

**Результат:** Градієнт від темно-синього до золотого

### Туторіал 2: Зробити картку святого об'ємною

**Мета:** Додати тінь та hover ефект

**Кроки:**

1. **Відкрийте** `src/pages/saints/index.astro`
2. **Знайдіть** картку святого (div з класом card)
3. **Клікніть** на неї
4. **У правій панелі** → Style → Effects
5. **Додайте:**
   ```
   shadow-lg
   hover:shadow-2xl
   transition-shadow
   duration-300
   ```
6. **Збережіть**
7. **Перевірте** — при наведенні миші тінь має збільшуватися

### Туторіал 3: Адаптувати текст під мобільний

**Мета:** Зменшити заголовок на мобільному

**Кроки:**

1. **Відкрийте** будь-яку сторінку
2. **Виберіть** заголовок (h1)
3. **Перемкніться** на Mobile view (кнопка зверху)
4. **У правій панелі** → Style → Typography → Font Size
5. **Замініть** `text-6xl` на:
   ```
   text-4xl md:text-5xl lg:text-6xl
   ```
6. **Збережіть**
7. **Перемикайте** між Mobile/Tablet/Desktop — текст має змінюватися

---

## 🚨 Типові проблеми та рішення {#troubleshooting}

### Проблема 1: Зміни не відображаються у браузері

**Симптоми:** Зберегли в Pinegrow, але сайт не оновився

**Рішення:**
1. Перевірте, чи працює dev server (в терміналі має бути `watching for file changes...`)
2. Оновіть браузер вручну (F5)
3. Якщо не допомагає — перезапустіть dev server:
   ```bash
   Ctrl+C (зупинити)
   npm run dev (запустити знову)
   ```

### Проблема 2: Pinegrow не бачить Tailwind класи

**Симптоми:** У правій панелі немає Tailwind опцій

**Рішення:**
1. Перевірте, чи у проекті є `tailwind.config.js`
2. У Pinegrow: `Project → Frameworks → Tailwind CSS`
3. Вкажіть шлях до `tailwind.config.js`
4. Перезавантажте Pinegrow

### Проблема 3: Сайт "зламався" після редагування

**Симптоми:** Білий екран або помилки в браузері

**Що зробили (найчастіше):**
- ❌ Видалили frontmatter блок (`---`)
- ❌ Видалили важливий компонент
- ❌ Зіпсували закриваючий тег

**Рішення:**
1. **Undo:** Ctrl+Z кілька разів
2. **Перевірте термінал** — там будуть помилки
3. **Попросіть розробника** відкотити через Git

### Проблема 4: Класи додаються, але нічого не змінюється

**Симптоми:** Додали `bg-red-500`, але фон не червоний

**Причина:** Конфлікт класів (інший клас перекриває)

**Рішення:**
1. **Видаліть старі класи** (наприклад, `bg-white`)
2. **Перевірте специфічність** — більш конкретні класи перекривають загальні
3. **У крайньому випадку** додайте `!important` (але це погана практика):
   ```
   !bg-red-500
   ```

### Проблема 5: Мобільна версія виглядає погано

**Симптоми:** На телефоні все зламане

**Рішення:**
1. **Перемкніться на Mobile view** у Pinegrow
2. **Додайте responsive класи:**
   ```
   text-2xl md:text-4xl    (текст)
   p-4 md:p-8              (відступи)
   flex-col md:flex-row    (напрямок)
   ```
3. **Перевірте ширину** — можливо елементи занадто широкі:
   ```
   max-w-full    (максимум 100%)
   w-full        (ширина 100%)
   ```

---

## 📚 Шпаргалка: Найчастіші класи

### Кольори (наш проект)

```css
/* Фон */
bg-primary-500      /* Золотий */
bg-secondary-900    /* Темно-синій */
bg-white            /* Білий */
bg-gray-50          /* Світло-сірий */

/* Текст */
text-primary-500    /* Золотий текст */
text-secondary-900  /* Темно-синій текст */
text-white          /* Білий текст */
text-gray-600       /* Сірий текст */
```

### Відступи

```css
/* Padding (всередині) */
p-4        /* 16px з усіх боків */
px-6       /* 24px зліва та справа */
py-8       /* 32px зверху та знизу */

/* Margin (зовні) */
m-4        /* 16px з усіх боків */
mx-auto    /* Центрування */
my-8       /* 32px зверху та знизу */
```

### Типографіка

```css
/* Розмір */
text-sm     /* 14px */
text-base   /* 16px */
text-xl     /* 20px */
text-2xl    /* 24px */
text-4xl    /* 36px */

/* Вага */
font-normal    /* 400 */
font-semibold  /* 600 */
font-bold      /* 700 */

/* Вирівнювання */
text-left
text-center
text-right
```

### Ефекти

```css
/* Тіні */
shadow-sm
shadow-lg
shadow-xl

/* Заокруглення */
rounded       /* 4px */
rounded-lg    /* 8px */
rounded-full  /* 50% */

/* Рамки */
border
border-2
border-primary-500
```

### Layout

```css
/* Flexbox */
flex
flex-col       /* Колонка */
flex-row       /* Ряд */
justify-center /* По центру */
items-center   /* По центру вертикально */
gap-4          /* Проміжок 16px */

/* Grid */
grid
grid-cols-2    /* 2 колонки */
grid-cols-3    /* 3 колонки */
gap-6          /* Проміжок 24px */
```

### Responsive

```css
/* Мобільний → Планшет → Desktop */
text-2xl md:text-4xl lg:text-6xl
p-4 md:p-8 lg:p-16
hidden md:block    /* Сховати на мобільному */
```

---

## 🎯 Чеклист перед здачею роботи

Перш ніж сказати "Готово!", перевірте:

- [ ] **Перевірив усі сторінки** (головна, про нас, святі, контакти)
- [ ] **Переглянув на мобільному** (Mobile view у Pinegrow)
- [ ] **Переглянув на планшеті** (Tablet view)
- [ ] **Переглянув на desktop** (Desktop view)
- [ ] **Всі кольори відповідають бренду** (золотий, синій, червоний)
- [ ] **Відступи рівномірні** (не занадто тісно, не занадто просторо)
- [ ] **Текст читабельний** (достатній контраст, нормальний розмір)
- [ ] **Зображення на місці** (не розтягнуті, не зіпсовані)
- [ ] **Кнопки працюють** (мають hover ефект)
- [ ] **Немає помилок у терміналі** (dev server працює чисто)
- [ ] **Зберіг усі файли** (Ctrl+S у Pinegrow)
- [ ] **Перевірив у браузері** (http://localhost:4321)

---

## 📞 Коли звертатися до розробника

**Зверніться до розробника, якщо:**

- ❗ Сайт "зламався" і Undo не допомагає
- ❗ Потрібно додати нову функціональність (форми, анімації)
- ❗ Потрібно інтегрувати з CMS (Keystatic)
- ❗ Потрібно налаштувати деплой на Vercel
- ❗ З'явилися помилки в терміналі
- ❗ Потрібно створити новий компонент
- ❗ Потрібно працювати з даними з бази/CMS

**Не бійтеся питати!** Краще запитати, ніж зіпсувати.

---

## 🎉 Вітаємо!

Ви тепер знаєте, як редагувати дизайн сайту храму в Pinegrow!

**Наступні кроки:**
1. Попрактикуйтеся на тестових сторінках
2. Спробуйте всі туторіали з цього гайду
3. Експериментуйте з кольорами та відступами
4. Не бійтеся натискати кнопки — завжди можна Undo!

**Корисні ресурси:**
- Tailwind CSS Docs: https://tailwindcss.com/docs
- Pinegrow Tutorials: https://pinegrow.com/tutorials
- Astro Docs: https://docs.astro.build

---

**Створено для проекту "Храм на честь Трьох святителів на Подолі"**
**Версія:** 1.0
**Дата:** 2026-02-03
