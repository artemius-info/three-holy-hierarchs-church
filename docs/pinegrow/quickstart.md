# ⚡ Швидкий старт: Pinegrow за 5 хвилин

## 🚀 Крок 1: Запустіть dev сервер

**У терміналі:**
```bash
cd D:\Projects\Three_Holy_Hierarchs
npm run dev
```

✅ Побачите: `Local http://127.0.0.1:4321/`

❗ **НЕ ЗАКРИВАЙТЕ** це вікно!

---

## 🎨 Крок 2: Відкрийте Pinegrow

1. Запустіть **Pinegrow 9**
2. `File → Open Project...`
3. Виберіть: `D:\Projects\Three_Holy_Hierarchs`
4. Двічі клікніть: `src/pages/index.astro`

---

## ✏️ Крок 3: Редагуйте

### Вибір елемента
- Клікніть на елемент на сторінці
- АБО клікніть у дереві зліва

### Зміна кольору
1. Права панель → **Style** → **Background**
2. Виберіть колір:
   - `bg-primary-500` — золотий
   - `bg-secondary-900` — темно-синій

### Зміна тексту
1. Виберіть текстовий елемент
2. Права панель → **Typography**
3. Змініть:
   - Font Size: `text-4xl`
   - Font Weight: `font-bold`
   - Color: `text-white`

### Відступи
1. Права панель → **Spacing**
2. Змініть:
   - Padding: `p-6` (всередині)
   - Margin: `m-4` (зовні)

---

## 💾 Крок 4: Збережіть

**Натисніть:** `Ctrl+S` (Windows) або `Cmd+S` (Mac)

**Перевірте:** Браузер на `http://localhost:4321` автоматично оновиться

---

## 📱 Крок 5: Перевірте мобільну версію

1. Зверху Canvas клікніть **[Mobile]**
2. Побачите, як виглядає на телефоні
3. Додайте responsive класи:
   ```
   text-2xl md:text-4xl lg:text-6xl
   ```

---

## ⚠️ Що НЕ чіпати

❌ **Frontmatter блок** (між `---` та `---`)
❌ **Динамічні змінні** (`{title}`, `{props.subtitle}`)
❌ **Імпорти** (`import ...`)

✅ **Можна редагувати:**
- HTML теги
- Tailwind класи (`class="..."`)
- Статичний текст

---

## 🎨 Найчастіші класи

### Кольори
```
bg-primary-500      /* Золотий фон */
text-secondary-900  /* Темно-синій текст */
```

### Відступи
```
p-4       /* Padding 16px */
m-4       /* Margin 16px */
mx-auto   /* Центрування */
```

### Текст
```
text-2xl        /* Розмір 24px */
font-bold       /* Жирний */
text-center     /* По центру */
```

### Ефекти
```
shadow-xl       /* Велика тінь */
rounded-lg      /* Заокруглення */
hover:bg-primary-600  /* Hover ефект */
```

---

## 🚨 Якщо щось не працює

### Зміни не відображаються
1. Перевірте dev server (має бути запущений)
2. Натисніть F5 у браузері
3. Перезапустіть server (Ctrl+C, потім `npm run dev`)

### Щось зламалося
1. Натисніть `Ctrl+Z` кілька разів
2. Якщо не допомогло — попросіть розробника відкотити через Git

---

## 📖 Детальна інструкція

Повний гайд (500+ рядків): `PINEGROW_GUIDE.md`

---

**Готово! Починайте експериментувати! 🎉**
